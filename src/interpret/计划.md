# 说明
1. 基于cpp 一个解释器 支持基本的脚本语法 语法类似F#和rust例如 let a=1 支持;和直接分行
2. 一个token_emitter 可以不断去除token 一边取出一边执行
3. 完全基于动态oop系统

# 例子代码


let a=1
let b=2
let c=a+b
print c
print "c={}" c

//可枚举对象 一个叫types的就是可枚举对象

list types //可以列出在oop类型表里注册的所有类型
list type testobj //列出testobj类的所有信息 包括desc


