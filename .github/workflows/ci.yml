name: ci

on:
  push:
  pull_request:

jobs:
  rocky-rpm:
    runs-on: ubuntu-latest
    container:
      image: openeuler/openeuler:24.03
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: 更新包缓存
        run: |
          dnf -y makecache
          dnf -y update

      - name: 安装构建工具
        run: |
          dnf -y install git gcc gcc-c++ cmake make curl unzip tar pkgconfig findutils ninja-build

      - name: 安装vcpkg
        run: |
          git clone https://github.com/microsoft/vcpkg
          ./vcpkg/bootstrap-vcpkg.sh
          ./vcpkg/vcpkg install catch2

      - name: Configure
        run: |
          cmake -G Ninja -S . -B build -DCMAKE_TOOLCHAIN_FILE=$PWD/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_BUILD_TYPE=Debug

      - name: Build
        run: |
          ninja -C build
